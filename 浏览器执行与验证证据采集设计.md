# 浏览器执行与验证证据采集设计

## 1. 文档定位

本文定义 `tsm-agt` 的浏览器执行、开发服务资源管理、浏览器事实采集和工件存储能力。

本文只解决一个问题：**当模型请求浏览器相关操作时，系统如何在受控边界内执行，并留下可追溯的运行事实。**

本文不定义：

- 用户需求是否已经满足；
- 任务是否完成；
- 下一步应验证什么；
- 验证失败后应如何修复；
- 模型应如何规划或重试；
- 投影页面如何从事实推断业务结论。

这些语义问题由《[Agent 行动与结论事实可追溯设计](Agent行动与结论事实可追溯设计.md)》定义的模型行动协议和上层消费方处理。

## 2. 背景

现有系统已经具备工具协议、任务作用域、审批、后台进程、事件记录、工作区变更记录与本地页面展示能力。但它缺少受控浏览器执行器，导致模型无法通过真实页面观察获得以下事实：

- 本地服务是否真正可访问；
- 指定页面是否加载；
- 页面结构中是否存在目标元素；
- 浏览器是否出现脚本报错；
- 是否存在请求失败；
- 页面是否发生横向或纵向溢出；
- 某次页面操作前后的页面状态是否改变；
- 截图、页面结构和日志是否属于当前任务。

浏览器能力的目标不是替模型判断“功能已修复”，而是补充模型可引用的真实运行事实。

## 3. 架构边界

```text
模型驱动的行动循环
    │ 请求工具调用
    ▼
Runtime / Kernel
    │ 执行、审批、资源管理、事件记录
    ▼
浏览器执行适配器 ───── 开发服务资源管理器
    │                         │
    ▼                         ▼
浏览器执行事实              进程与就绪事实
    │                         │
    └──────── 工件存储 ───────┘
                 │
                 ▼
任务事件与受控读取接口
                 │
                 ▼
投影层仅展示事实、工件和状态
```

职责边界如下：

| 组件 | 允许职责 | 禁止职责 |
|---|---|---|
| 模型行动循环 | 结合上下文选择下一步工具调用、解释已有事实 | 伪造执行事实 |
| Runtime / Kernel | 授权、执行、隔离资源、持久化事件和工件引用 | 语义规划、完成判定、恢复规划 |
| 浏览器执行适配器 | 驱动浏览器、收集浏览器原始事实 | 判断用户需求是否满足 |
| 开发服务资源管理器 | 启动、探测就绪、读取状态与关闭进程 | 以固定等待时间假设服务可用 |
| 工件存储 | 保存、校验、关联、脱敏和读取工件 | 解释工件业务含义 |
| 投影层 | 查询并呈现事实和工件 | 从文本或工件推断成功、失败或完成 |

## 4. 目标与非目标

### 4.1 目标

1. 让 Runtime 以工具提供者形式暴露浏览器执行能力。
2. 浏览器资源必须按任务隔离。
3. 开发服务必须具有明确的所属任务、就绪判定、最大生命周期和清理策略。
4. 截图、页面结构、浏览器报错、请求失败和布局信息必须以工件或结构化事实保存。
5. 所有工件必须能关联任务、工具调用、时间、哈希和脱敏状态。
6. 页面和本地接口只能读取工件元数据与受控内容，不通过临时进度通道传输大对象。

### 4.2 非目标

1. 不实现桌面级鼠标键盘控制。
2. 不实现自动修复循环。
3. 不实现视觉基线管理和视觉差异判定；该能力在事实采集稳定后另行设计。
4. 不定义“页面正确”或“任务完成”的语义规则。
5. 不让浏览器适配器持有跨任务共享的登录态、页面或存储数据。

## 5. 现有系统接入点

本设计复用现有边界，不新增平行 Runtime 编排核心。

| 现有组件 | 接入方式 |
|---|---|
| `ToolSpec` 与 `ToolProviderPort` | 浏览器能力以新的工具提供者接入 |
| `ToolInvocationContext` | 使用 `task_id`、工作区、审批、授权风险和进程控制能力 |
| `Kernel` 后台进程能力 | 管理开发服务的启动、状态、日志、停止与任务结束清理 |
| Runtime 事件存储 | 记录浏览器会话、服务状态、工件创建和工具结果事实 |
| 本地事件接口 | 提供任务作用域的工件元数据查询与受控下载 |
| Web 投影 | 展示事实、截图、报错和服务状态，不作语义推理 |

原有 `ToolSpec` 中的风险、网络访问、数据外发、幂等性、回滚说明和结果权威字段继续是唯一的策略来源。不得新增独立的权限注册表作为第二权威来源。

## 6. 资源模型

### 6.1 浏览器会话

浏览器资源按任务隔离，而非按 Agent 服务进程共享。

```text
任务
├── 浏览器会话
│   ├── 浏览器上下文
│   ├── 页面集合
│   ├── 存储与 Cookie
│   ├── 控制台记录
│   └── 请求失败记录
└── 开发服务租约（可选）
```

浏览器会话至少包含：

```text
会话编号
所属任务编号
创建时间
最后活动时间
最大存活时间
页面数量
允许访问范围
当前状态
清理结果
```

约束：

- 不同任务不得共享浏览器上下文；
- 任务终止、超时或取消时必须清理页面和上下文；
- Agent 服务重启后，不假设旧浏览器会话仍可用；
- 恢复任务时，应重新建立会话并把旧会话失效记录为事实；
- 同一任务的多个页面必须显式归属同一会话。

### 6.2 开发服务租约

开发服务不是“随手启动的后台命令”，而是受任务控制的资源。

```text
服务编号
所属任务编号
后台进程编号
启动命令摘要
工作目录
监听端口或健康地址
就绪策略
创建时间
最大生命周期
任务结束是否自动关闭
当前状态
```

就绪策略至少支持：

- HTTP 健康地址返回预期状态；
- 指定端口可连接；
- 进程日志出现指定就绪标记；
- 显式禁止固定等待若干秒作为生产级就绪判定。

开发服务启动成功仅表示进程已创建；服务就绪必须由独立事实事件记录。

## 7. 浏览器工具表面

第一期只提供最小、可组合的工具，不把高层业务流程写入浏览器适配器。

| 工具 | 作用 | 主要事实输出 |
|---|---|---|
| `browser.open` | 打开地址或新建页面 | 页面编号、最终地址、加载状态 |
| `browser.close` | 关闭页面或会话 | 清理结果 |
| `browser.navigate` | 导航到地址 | 最终地址、加载状态 |
| `browser.click` | 点击已定位元素 | 操作结果、操作前后摘要 |
| `browser.type` | 向已定位元素输入 | 操作结果、输入长度摘要 |
| `browser.dom_snapshot` | 获取受控页面结构快照 | 工件编号、结构摘要 |
| `browser.screenshot` | 获取截图 | 工件编号、尺寸、哈希 |
| `browser.console` | 读取控制台记录 | 结构化日志事实 |
| `browser.network_failures` | 读取请求失败记录 | 结构化失败事实 |
| `browser.layout` | 读取指定元素布局信息 | 尺寸、滚动与溢出事实 |

工具参数必须使用稳定的页面编号、元素定位表达式和任务作用域资源编号。模型不得直接获得底层浏览器对象。

## 8. 浏览器事实与工件

### 8.1 事实事件

浏览器相关事件只描述发生过的事情，例如：

```text
browser.session_created
browser.page_opened
browser.navigation_completed
browser.action_completed
browser.dom_captured
browser.screenshot_captured
browser.console_captured
browser.network_failures_captured
browser.layout_captured
browser.page_closed
browser.session_closed
browser.session_cleanup_failed
```

事件不得包含以下语义字段：

```text
功能已修复
页面符合需求
任务已完成
应当重试
下一步应如何处理
```

### 8.2 工件记录

截图、完整页面结构、控制台原始记录、网络失败详情等可能体积大或包含敏感信息，不应直接放入进度事件。

每个工件至少记录：

```json
{
  "artifact_id": "artifact-...",
  "task_id": "task-...",
  "tool_call_id": "call-...",
  "kind": "browser_screenshot",
  "mime_type": "image/png",
  "storage_path": "受控存储路径",
  "sha256": "内容哈希",
  "size_bytes": 0,
  "created_at": "时间",
  "redaction_status": "未脱敏/已脱敏/禁止展示",
  "summary": "不含敏感内容的摘要"
}
```

工件内容读取必须满足：

- 调用方持有当前本地接口的认证凭证；
- 工件属于请求的任务；
- 工件状态允许展示；
- 大对象使用受控读取或下载接口；
- 页面结构、请求地址、控制台内容按策略脱敏。

## 9. 安全与策略

浏览器执行能力必须进入现有工具风险和审批体系。

重点策略：

1. 默认只允许访问任务声明或当前工作区派生出的本地地址。
2. 外部地址访问按网络策略和审批策略处理。
3. 上传、下载、表单提交、身份登录和外部数据发送必须有独立风险说明。
4. 页面结构、截图、请求信息和控制台日志默认视为可能包含敏感数据。
5. 浏览器会话不得继承其他任务的 Cookie、缓存或存储状态。
6. 开发服务命令继续经过现有进程沙箱、路径校验和进程生命周期控制。

## 10. 本地接口与页面展示

本地接口和页面只能消费下列信息：

```text
任务关联的浏览器会话摘要
开发服务状态
浏览器事实事件
工件元数据
受控工件内容
```

页面应提供：

- 截图缩略图和全图预览；
- 页面结构摘要；
- 浏览器报错与请求失败列表；
- 开发服务状态和日志入口；
- 工件来源的工具调用与时间；
- 工件是否脱敏、是否可读取。

页面不得根据“截图存在”“服务启动成功”“工具调用成功”等事实自动渲染“功能已修复”或“任务已完成”。

## 11. 测试策略

### 11.1 单元测试

- 会话与任务隔离；
- 会话超时和任务取消后的清理；
- 工件哈希、归属和读取授权；
- 端口就绪、健康检查和服务退出诊断；
- 工具参数校验和风险策略；
- 事件中不存在完成判定或恢复规划字段。

### 11.2 集成测试

- 启动最小本地服务后打开页面；
- 采集页面结构、截图、控制台和请求失败；
- 验证工件可被同任务读取、不可被其他任务读取；
- 验证任务结束后服务和浏览器资源被释放；
- 验证服务启动失败、页面加载失败和清理失败均产生可追溯事实。

### 11.3 页面测试

- 工件预览不直接依赖进度事件的大对象；
- 页面只展示事实和工件元数据；
- 页面不从文本猜测成功、失败或完成状态。

## 12. 分期

### 第一期

- 任务级浏览器会话；
- 开发服务租约和真实就绪探测；
- 页面打开、页面结构、截图、控制台、请求失败采集；
- 工件存储与任务归属；
- Runtime 事件与受控读取接口；
- 基础页面展示。

### 第二期

- 布局数据采集；
- 多页面操作记录；
- 工件脱敏策略细化；
- 更完整的服务诊断与资源配额。

### 后续独立设计

- 视觉基线与差异分析；
- 浏览器回放；
- 自动修复；
- 多执行沙箱；
- 桌面操作。

## 13. 验收标准

本设计交付后，应能证明以下执行事实能力存在：

1. 不同任务的浏览器会话、存储状态和工件互相隔离。
2. 开发服务启动后必须产生独立的就绪事实，不以固定等待替代。
3. 截图、页面结构、控制台和请求失败能够关联到具体任务和工具调用。
4. 工件可校验、可脱敏、可受控读取。
5. Runtime 事件只记录执行事实，不出现任务完成、语义验收或恢复规划结论。
6. 页面仅展示事件和工件，不从事实自行推断业务完成状态。
