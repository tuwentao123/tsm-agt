# tsm-agt 通用任务执行闭环实施表

目标：建立统一的 TaskSpec → Outcome → Action → ToolResult → Completion / Continuation 闭环。

适用范围：普通问答、只读分析、代码修改、命令执行、后台进程、文档产物、分阶段任务、中途转向和中断恢复。多阶段 SPEC 只是其中一种组合，不使用专用流程。

## 架构不变量

1. Runtime 管理确定性状态、权限和验收；自然语言关系由可替换模型 Adapter 判断。
2. 模型只能提交 Proposal、Tool Call 和建议，不能直接写 Runtime 权威完成状态。
3. 普通文本中的“准备做”“下一步做”不能证明 Outcome 已完成。
4. 只有持久化 ToolResult、Evidence、Mutation、Process 状态、用户决定或验收事件可以关闭必需 Outcome。
5. Outcome 不授予新权限，审批、Project Trust、Sandbox 和工作区边界保持原样。
6. 旧 Event、旧 Checkpoint 和旧 Task 必须可读取；无法证明的旧工作不伪造为已完成。
7. macOS 与 Windows 使用同一 Port、Event 和状态模型，平台差异留在 Adapter。

## 执行批次

| 批次 | 实施内容 | 主要产物 | 验收门槛 | 状态 |
|---|---|---|---|---|
| B1 | Proposal 协议与 Runtime Outcome 领域模型 | TaskSpecProposal、JSON Schema v1、TaskOutcomeSnapshot、旧 Event 兼容 | Task Spec 专项和回放通过 | 已完成 |
| B2 | 可插拔 Planner 与 Intake 接线 | TaskSpecPlannerPort、Tool-Call Adapter、有界纠错、正式 Snapshot | Planner 专项；失败不弱化验收 | 已完成 |
| B3 | Action 与 Outcome 关联和投影 | outcome_ref、Action/Event 关联、Outcome 投影 | 各类 ToolResult 可追踪 | 已完成 |
| B4 | Completion 与 Final Acceptance 闭环 | REQUIRED_OUTCOME_UNSATISFIED、task-outcome-fulfillment、无进展保护 | 只承诺不执行不可成功 | 已完成 |
| B5 | PendingUserAction、Continuation 与输入关系 | 通用等待点、关系解析、Steer/Replace/Queue 修订 | 常见多轮交互走结构化状态 | 已完成 |
| B6 | Checkpoint、上下文、迁移和全场景回归 | Outcome 上下文、恢复、CLI 展示、真实模型验证 | 专项、全量、真实链路通过 | 已完成 |

## B1：协议与领域模型

- [x] 定义通用 TaskOutcomeKind 和 Runtime 权威 TaskOutcomeStatus。
- [x] 定义模型可填写的 TaskOutcomeProposal 与 TaskSpecProposal。
- [x] 定义版本化 TASK_SPEC_PROPOSAL_SCHEMA_V1。
- [x] 模型不可填写 task_id、revision、status、fulfillment_refs、content_hash。
- [x] 扩展 TaskSpecSnapshot 保存 Outcome 和 Continuation Policy。
- [x] 旧 Event 缺少新字段时确定性回放。
- [x] 新字段纳入内容哈希与序列化。

## B2：Planner 与 Intake

- [x] 新增 TaskSpecPlannerPort，只返回无权限 Proposal。
- [x] Tool Calling Provider 使用 planner.submit_task_spec。
- [x] Tool Calling Provider 走严格工具协议；纯文本 Provider 走同一 Schema 的严格 JSON，并各自只允许一次纠错。
- [x] 非法输出只允许一次有界纠错。
- [x] Runtime 校验 Proposal 后生成正式 Snapshot。
- [x] Planner 失败时保存可恢复失败，不退化成仅有 workspace-integrity。

## B3：Action 与 Outcome

- [x] Tool Call 协议增加稳定 outcome_ref。
- [x] 单一活动 Outcome 可安全自动绑定，多候选时不猜测。
- [x] Action Requested、Policy、Approval、Tool Started/Completed/Failed 与 Outcome 建立同一 Event 引用；审批跨 SQLite 重启不丢引用。
- [x] Evidence、Workspace/Artifact Mutation、Command、Process Control 和 User Decision 形成 fulfillment；失败/超时结果不形成 fulfillment。
- [x] 模型不能直接把 Outcome 标记为 DELIVERED。

## B4：完成与验收

- [x] Completion Readiness 从未关闭 Outcome 生成通用 Gap。
- [x] Gap 使用 ToolEffect 和候选工具能力匹配。
- [x] 第一次无实际行动时继续采样并保持工具开放。
- [x] 连续无结构化进展时有界阻塞。
- [x] Final Acceptance 增加 task-outcome-fulfillment。
- [x] workspace-integrity 不再代替目标完成证明。

## B5：等待与输入关系

- [x] PendingUserAction 统一表示 Approval、Clarification（含用户决定）和 Continuation；它是结构化等待状态，不解析普通文本。
- [x] 工作完成一部分后进入 AWAITING_USER，而不是 SUCCEEDED。
- [x] Runtime 只处理审批、等待、Checkpoint 等确定性协议状态。
- [x] Session Resolver 解析 New/Resume/Clarify；运行中模型分类器解析 Steer/Replace/Queue/Status，低置信度保持 Ambiguous。
- [x] 中断/取消和审批属于显式控制协议（Ctrl+C、CLI/SDK 决定），普通自然语言不会被模型静默提升为取消或授权操作。
- [x] Runtime 校验 Task、Session、状态、权限和置信度。
- [x] 不使用“继续”“确认”等关键词表。

## B6：恢复与回归

- [x] Checkpoint 保存 Task Spec revision、活动 Outcome 和 PendingUserAction。
- [x] 上下文压缩不能丢 Outcome、Action 和等待点。
- [x] Ctrl+C、超时、重启和升级后恢复同一工作状态。
- [x] 覆盖问答/分析（ANSWER、EVIDENCE）、修复/文档产物（WORKSPACE/ARTIFACT_DELIVERY）、命令、进程、用户决定、转向和历史继续。
- [x] 覆盖通用多阶段任务从读取、修改、命令验证、审批、中断、SQLite 重启到最终验收；多阶段 SPEC 复用同一闭环，无专用分支。
- [x] macOS 全量和真实模型验证；Windows 路径实现并记录原生验证状态。

## 完成证据（2026-09-10）

- 新增 Planner/Outcome/Continuation 专项，覆盖非法 Proposal 一次纠正、只承诺不执行、多个 Outcome 不乱绑、分段等待、普通输入恢复同一 Task，以及 SQLite 完整重启。
- Planner 已验证 Tool Call 和纯文本严格 JSON 两种 Provider 路径；两者复用同一个 TaskSpecProposal Schema 和 Runtime 校验，不引入平行合同。
- Outcome Effect 矩阵直接验证 OBSERVE、MUTATE（Workspace/Artifact）、EXECUTE、CONTROL、INTERACT；失败和超时保持未完成。自动绑定后的同一 outcome_ref 出现在 requested、policy、approval、started、completed/failed 全链路，审批跨 SQLite 重启仍一致。
- 通用编码端到端用三段 Outcome 验证“读文件 → 审批修改 → 意外中断 → SQLite 重启 → 审批运行测试 → Final Acceptance”；Replace 专项验证旧 Outcome 清空后由 Planner 生成新 Outcome。
- Session 的 `recent_task_summaries` 现在携带 Task Spec revision、Continuation Policy、Outcome 状态、所需能力和 fulfillment 数量；旧摘要没有这些字段时按空值读取。
- Checkpoint 的 Task Spec、活动 Outcome 和 PendingUserAction 字段纳入完整性哈希；旧 Checkpoint 缺字段仍按旧哈希确定性加载。
- `AFTER_COMPLETED_UNIT` 只由结构化 Outcome 变化触发；Runtime 不读取“继续/确认”等自然语言关键词。下一条普通输入仍由可替换 Session Resolver 判断续接、转向或新任务。
- 最新全量回归 725 项：沙箱内功能失败 0，5 项只因禁止绑定本机端口；对应 Ctrl+C 与 Local Event API 在沙箱外补跑 6/6 通过；7 项平台条件跳过。
- 真实私有模型隔离验证通过：Planner 生成 ANSWER Outcome，执行 `core.find_files → core.read_file`，共 3 次模型采样、2 次工具调用，返回精确虚构 marker；answer-completeness、task-outcome-fulfillment、final-evidence-integrity、workspace-integrity 四项验收全部通过。
- macOS 已完成编译、专项、全量、SQLite 重启和 Ctrl+C 验证。Windows 使用同一 pathlib/Port/Event/SQLite 代码路径，本轮没有 Windows 原生机器，因此仅完成实现兼容，未做原生运行验证。

## 独立能力边界

图片附件、联网搜索、MCP、跨机器恢复需要各自 Adapter 或资源协议。它们未来可以复用本闭环，但不与本轮任务状态实现混为一项。
