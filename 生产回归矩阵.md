# tsm-agt 生产回归矩阵

这份矩阵的目的不是追求单测数量，而是保证一个真实任务能从用户输入一直走到可解释、可恢复、可验收的结果。测试使用合成项目和假数据，不把现有仓库源码、业务源码、用户数据或密钥发送给外部模型。

## 第一层：每次修改必须通过

运行确定性生产旅程：

```bash
.venv/bin/python -m unittest -v tests.test_production_journeys
```

至少覆盖：

| 生产旅程 | 必须验证的链路 |
|---|---|
| 项目分析 | Planner → SQLite TaskSpec → 搜索/读取 → 最终回答 → Outcome 验收 → SQLite 重启恢复 |
| 错误工具计划自动纠正 | 模型错误绑定副作用工具 → Runtime 不执行 → 结构化纠正 → 只读取证 → 正常完成 |
| 持续协议错误 | 连续错误计划不执行工具 → 正确标为 `tool_protocol` → CLI 不误报网络故障 |
| 多轮工具后自动恢复 | 三次工具成功 → 同一逻辑回合首次物理响应无效 → 安全重采样 → 工具不重复 → 正常验收 |
| 多轮工具后恢复耗尽 | 已有 Evidence 不丢 → Checkpoint 保留 → Task 进入 `INTERRUPTED` → CLI 不误报网络或工具失败 |
| 持久化候选选择 | 展示多个可恢复 Task → 保存稳定编号 → CLI 重启 → 输入“第四个吧” → 不调用语义模型 → Runtime 校验并恢复第 4 项 |

改动涉及的专项测试也必须运行。验证失败时先修复，不以“只是测试问题”跳过。

## 第二层：合并前必须通过

```bash
git diff --check
.venv/bin/python -m compileall -q src tests
.venv/bin/python -m unittest discover -s tests
```

同时检查架构依赖方向、Git 忽略规则和工作区状态。全量回归中的平台条件跳过必须有明确原因；不能把真实失败改成无条件跳过。需要监听本机端口或被沙箱限制的测试，在获得授权后于沙箱外补跑。

## 第三层：Provider 或 Agent 主链改动必须通过

以下改动必须额外运行真实 Provider 合成旅程：Provider/HTTP/SSE、模型配置、Prompt、Planner、工具 Schema、Agent Loop、Completion Readiness、Outcome 或最终验收。

```bash
TSM_AGT_RUN_REAL_PROVIDER_JOURNEY=1 \
TSM_AGT_REAL_PROVIDER_ENV_FILE=/absolute/path/to/private/.env \
.venv/bin/python -m unittest -v tests.test_real_provider_journey
```

该测试默认跳过，避免离线 CI 访问网络。它只创建并发送合成临时项目，验收标准是：

1. 真实 Planner 生成有效 TaskSpec；
2. 真实模型至少调用一次工作区工具；
3. 回答包含合成项目的精确入口和 marker；
4. 所有 required Outcome 均持久化为 `DELIVERED`；
5. 最终验收为 `PASSED`；
6. Event Log 不含 `llm.failed` 或 `turn.failed`。

## 生产场景覆盖清单

后续新增能力时，应优先把下面场景组合成旅程，而不是只增加孤立函数测试：

- 普通知识问答，不需要工具；
- 当前项目分析，需要多次搜索和读取；
- 外部目录取证，需要范围解析和授权；
- 写文件前审批、Mutation Journal、写后验证；
- 命令执行、超时、取消、未知执行结果和恢复；
- 中断后恢复、跨 Task 指代、Steer、Queue、Replace；
- 候选菜单编号/ID 本地选择、SQLite 重启恢复、模糊语义回退与短超时心跳；
- 用户拒绝审批或回答澄清；
- 模型输出错误工具协议后的有限纠正；
- Provider 限流、5xx、读取超时、流式不兼容与非流式回退；
- Provider 物理 attempt 与 Agent model round 分离，重试不重复工具、不额外消耗逻辑回合；
- 上下文接近阈值后的压缩和 SQLite 重启恢复；
- macOS 和 Windows 的路径、锁、进程及信号差异。

每条旅程必须同时断言用户可见结果、工具是否真的执行、Event Log、Task/Outcome 状态和重启后的投影；只断言一段输出文字不算完成生产验证。
